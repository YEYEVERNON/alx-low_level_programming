#include "main.h"
/**
 * print_chessboard - entry
 * @a: array
 * Return: Always 0 (success)
 */
void print_chessboard(char (*a)[8])
{
	int 1, n;

	for (1 = 0; n < 8; n++)
	{
		_putchar(a[i][n]);
	_putchar('\n');
	}
}
