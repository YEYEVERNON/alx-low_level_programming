#include <stdio.h>
/**
 * main - Entry Point
 * Return : Always 0 Success
 */

int main(void)
{
        int n = 97;
	int m = 65;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	while (m <= 98)
	{
		putchar(m);
		n++;
	}
	putchar('\n');
	return (0);
}
