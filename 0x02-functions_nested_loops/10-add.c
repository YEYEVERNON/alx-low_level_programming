#include "main.h"

/**
 * Add - Adds two intergers and returns the result
 *
 * @x : nuber being added
 *
 * @y : number being added 
 *
 * Return : Always 0
 *
 */
int add(int x, int y)
{
	return (x + y);
}

